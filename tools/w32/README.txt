The MDT Python libraries are automatically set up to work with any version
of Python from 2.3 to 3.2. (You need to download Python separately from
www.python.org and install it.) One easy way to run MDT Python scripts is
to open them in Python's IDLE program and run them by pressing F5.
